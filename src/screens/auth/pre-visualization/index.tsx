import { SafeAreaView, ScrollView, View } from "react-native";

export function PreVisualization() {
    return (
        <SafeAreaView>
            <ScrollView>
                <View>

                </View>
            </ScrollView>
        </SafeAreaView>
    )
}